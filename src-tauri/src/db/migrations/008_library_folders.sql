-- Библиотека 2.0: одна карта (набор) = один объект; папки коллекций становятся
-- деревом без ограничения на глубину.
--
-- Папка ссылается на родительскую папку. Внешний ключ с ON DELETE SET NULL
-- задаёт поведение удаления: при удалении папки её подпапки и коллекции
-- поднимаются на уровень выше — структура вокруг них не теряется.

ALTER TABLE folders ADD COLUMN parent_id INTEGER REFERENCES folders(id) ON DELETE SET NULL;

-- Больше никакого переключателя «Наборами»: библиотека всегда показывает
-- наборы. Старыe сохранённые фильтры (умные коллекции) содержат groupSets —
-- код его просто игнорирует, колонки в базе у фильтра нет.
